name: feature
description: A feature brick for Mason  which is a tool for generating code from bricks and templates. Following the architecture of MVVM and Clean Architecture.
version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev.52 <0.1.0"

vars:
  name:
    type: string
    description: Your name
    default: new_feature
    prompt: What is your new feature name ?
